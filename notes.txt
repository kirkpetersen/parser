Parser notes

TODO
- cleanup current <empty>/tokenizer changes
- possibly introduce full (but simple) tokenizer
- improve the tokenizer to support at least basic functionality
- introduce the empty string notation and logic
