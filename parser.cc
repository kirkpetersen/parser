/*
 * parser.cc
 */

#include <assert.h>
#include <ctype.h>
#include <stdlib.h>
#include <string.h>

#include <iostream>
#include <queue>

using namespace std;

int main(int argc, char * argv[])
{
    // simple bottom up parser
    // read grammar from stdin
    // very simple tokenizer
    // upper/lower for terminal/nonterminal
    // STL for data structures
    // proof of concept of my dynamic parser idea
    // - most importantly, can the grammar be modified on the fly?

    return 0;
}
